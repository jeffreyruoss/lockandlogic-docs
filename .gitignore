node_modules/
**/.vitepress/dist/
**/.vitepress/cache/
.DS_Store

# Local scratch — client spreadsheets etc., not part of the docs site
temp/

# MCP config (contains API keys)
.mcp.json

# Claude Code (track .claude/CLAUDE.md if created)
.claude/
!.claude/CLAUDE.md
